#include <bits/stdc++.h>
using namespace std;
int main()
{
    int num = 0;
    cout << "Enter a sorted array " << endl;
    cin >> num;
    int arr[num];

    int beg = 0, end = 0;

    for (int i = 0; i < num; i++)
    {

        cin >> arr[i];
    }

    beg = 0;
    end = n - 1;
    while (beg != end)
    {
        if (arr[beg] + arr[end])
        {
            /* code */
        }
    }

    return 0;
}